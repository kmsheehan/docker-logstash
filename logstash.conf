input {
  stdin {
    type => "stdin-type"
  }

  tcp {
    port => 5000
    type => syslog
  }

  udp {
    port => 5000
    type => syslog
  }
}

filter {

output {
  stdout {
    codec => rubydebug
  }

  elasticsearch {
    embedded => true
  }
}
